const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 300,
    disponible: true,
};

console.log(Object.keys(producto)); //Devuelve un array con las propieddes del objerto
console.log(Object.values(producto)); //Devuelve un array con los valores
console.log(Object.entries(producto));// Devuelve todo

